// Where the SHOP menu leads. While this is empty the menu keeps opening this site's own /shop page.
// Once the farm shop site is deployed, put its address here, for example "https://farm.example.com".
export const SHOP_URL = "";
